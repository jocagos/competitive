#include <iostream>
#include <algorithm>
#include <bitset>

using namespace std;
constexpr int MAXMN = 1024;

int main(){
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  int n {}, m {};
  cin >> n >> m;
  bitset<MAXNM> bitmask( 0 );
  for( int i = 0; i < (1 << (m * n)); ++ i ){
    bitmask
  }
  return 0;
}
