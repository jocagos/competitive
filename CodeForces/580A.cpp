#include <iostream>

using namespace std;

int main(){
  int n, length = 1;
  scanf("%d", &n);
  if( n == 1 ){ cout << 1 << endl; return 0; }
  int prev, /* to store previous element and compare between scans*/
    now; // To store actual element and compare
  
  return 0;
}
