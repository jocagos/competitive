#include <iostream>
#include <cmath>

using namespace std;

long long double_dip( long long n, long long k ){
  long long p = k - n;
  while( 
}

int main(){
  int tc {};
  cin >> tc;
  while( tc -- ){
    long long n {}, k {};
    cin >> n >> k;
    
  }
  return 0;
}
