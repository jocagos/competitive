#include <bits/stdc++.h>

using namespace std;

int bitLXor( int a, int b ){
  int l = ( b == 1 ? 1 : static_cast<int>(ceil(log2(b))));
  cout << "l is " << l << endl;
  return 0;
}

int main(){
  bitLXor( -1, 1 );
  return 0;
}
