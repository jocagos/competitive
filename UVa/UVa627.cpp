#include <bits/stdc++.h>
#define range( var, MAX ) for( int var = 0; var < MAX; ++ var )
#define range( MAX ) range( _, MAX )
using namespace std;

constexpr int MAXN = 330;

int main(){
  int elligent, erpolate;
  string ify;
  getline( cin, ify );
  elligent = stoi( ify );
  range( elligent ){
    getline( cin, ify );
    
  }
  return 0;
}
