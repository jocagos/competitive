#include <iostream>

using namespace std;

int main(){
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  int tc {};
  cin >> n;
  while( tc -- ){
    long long n {};
    cin >> n;
    for( long long i = 1; i <= 1000000LL; ++ i ){
      if( i * 
    }
  }
  return 0;
}
