#include <iostream>
#include <cstring>
using namespace std;

int main(){
  int t, coins[101];
  scanf("%d ", &t);
  while(t--){
    int n, k;
    memset(coins, 0, sizeof coins);
    scanf("%d %d", &n, &k);
    
  }
  return 0;
}
