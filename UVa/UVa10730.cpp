#include <iostream>
#include <string>
constexpr int MAXN = 10010;

using namespace std;

int main(){
  int n {};
  int a[MAXN] {};
  while( scanf("%d:", &n), n ){
    for( int i = 0; i < n; ++ i ){
      scanf("%d", &a[i]);
    }
    for( int i = 0; i < n; ++ i ){
      for( int j = i + 1; j < n; ++ j ){
	
      }
    }
  }
  return 0;
}
