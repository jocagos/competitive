#include <bits/stdc++.h>

using namespace std;
constexpr int MAXN = 22;
int n {}, r {};
vector<int> zones( MAXN );
map<>

int main(){
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  while( cin >> n >> r, n | r ){
    for( int i = 0; i < n; ++ i )
      cin >> zones[i];
    int m;
    cin >> m;
    for( int i = 0; i < m; ++ i ){
      int k;
      cin >> k;
      
    }
  }
  return 0;
}
